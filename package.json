{
  "description": "BEIS regional indicators",
  "license": "MIT",
  "name": "beis_indicators",
  "scripts": {
    "deploy": "cd ui && npm run makedata && npm run export",
    "postinstall": "cd ui && npm install"
  }
}
