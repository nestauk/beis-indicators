import regionSpecs from '@svizzle/atlas/src/specs';

export const toolName = 'Nesta_BEIS_indicators';

/* navigation */

export const hrefBase = '/indicators';

/* flags */

export const flags = {
	showPOIs: true
}

/* regional selection */

const regionType = 'NUTS';

export const regionSettings = {
	canSelectLevels: false,
	filterableLevel: 1,
	ignoredRegions: [],
	key: 'NUTS_ID',
	level: 2,
	level0: 'UK',
	levels: regionSpecs[regionType].levels,
	objectId: 'NUTS',
	resolution: '03M',
	type: regionType,
}
export const isDev = process.env.NODE_ENV === 'development';
