import { writable } from 'svelte/store';

import {makeIndicatorsLookup} from 'app/utils';
import indicatorsIndex from 'app/data/indicatorsIndex.json';

const lookup = makeIndicatorsLookup(indicatorsIndex);

export const lookupStore = writable(lookup);
